import * as T from 'three';import {createModel,xy} from './model.js';const m=createModel();m.root.updateMatrixWorld(true);const ps=m.tables.filter(t=>t.zoneId==='prato-back');const bs=ps.map(t=>new T.Box3().setFromObject(t.group));let ok=true;for(let i=0;i<ps.length;i++)for(let j=i+1;j<ps.length;j++)if(bs[i].intersectsBox(bs[j])){console.log('OVERLAP',ps[i].id,ps[j].id);ok=false;}const tree=xy(103,125);for(let i=0;i<ps.length;i++){const b=bs[i];if(tree.x>b.min.x-.2&&tree.x<b.max.x+.2&&tree.z>b.min.z-.2&&tree.z<b.max.z+.2){console.log('TRUNK',ps[i].id);ok=false;}}console.log({ok});if(!ok)process.exitCode=1;
